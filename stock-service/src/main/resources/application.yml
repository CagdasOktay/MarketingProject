server:
  port: 5051

spring:
  datasource:
    username: 'postgres'
    password: 'root'
    driver-class-name: org.postgresql.Driver
    url: 'jdbc:postgresql://localhost:5432/marketingStockDB'

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true